# Configuración de maquinas

Para estandarizar la configuración de maquinas para la clase de computación de alto desempeño, vamos a utilizar scripts bash para estandarrizar el proceso, iniciaamnete se va a ejecutar el archivo BasicConf.sh como administrador.

```bash
sudo ./BasicConf.sh -n NOMBRE_MAQUINA
```
